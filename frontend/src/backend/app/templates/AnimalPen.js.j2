// Auto-generated {{ animalName }}Pen.js
// Generated by CreatureStudio export_service. Do not edit by hand.

import * as THREE from "three";
import { createStandardMaterial } from "../../renderkit/materialUtils.js";
import create{{ animalName }}Behavior from "./{{ animalName }}Behavior.js";

/**
 * Create a simple pen / enclosure scene for a {{ animalName }}.
 *
 * This is intentionally lightweight. For the full Zoo project you can replace
 * this with a more detailed environment while keeping the public API the same.
 */
export function create{{ animalName }}Pen(scene, options = {}) {
  const group = new THREE.Group();
  scene.add(group);

  // Basic ground plane.
  const groundGeometry = new THREE.CircleGeometry(5, 64);
  const groundMaterial = createStandardMaterial({
    color: 0x555544,
    roughness: 0.9,
    metalness: 0.0,
  });
  const ground = new THREE.Mesh(groundGeometry, groundMaterial);
  ground.rotation.x = -Math.PI / 2;
  ground.receiveShadow = true;
  group.add(ground);

  const behavior = create{{ animalName }}Behavior(options);
  group.add(behavior.root);

  function update(dt) {
    behavior.update(dt);
  }

  return {
    group,
    behavior,
    update,
  };
}

export default create{{ animalName }}Pen;
